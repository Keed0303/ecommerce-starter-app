# Email Verification Flow

## 📊 System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                     EMAIL VERIFICATION FLOW                         │
└─────────────────────────────────────────────────────────────────────┘

1. USER REGISTRATION
   ┌──────────────┐
   │   Register   │
   │    Form      │
   └──────┬───────┘
          │
          ▼
   ┌──────────────────────┐
   │  User submits form   │
   │  POST /register      │
   └──────┬───────────────┘
          │
          ▼

2. EVENT TRIGGERED
   ┌──────────────────────────────────────┐
   │  Illuminate\Auth\Events\Registered   │
   │  event is fired                      │
   └──────┬───────────────────────────────┘
          │
          ▼
   ┌──────────────────────────────────────────────┐
   │  SendEmailVerificationNotification           │
   │  listener catches the event                  │
   └──────┬───────────────────────────────────────┘
          │
          ▼

3. EMAIL GENERATION
   ┌──────────────────────────────────────┐
   │  Generate signed verification URL     │
   │  - User ID                            │
   │  - Email hash                         │
   │  - 60-minute expiration               │
   │  - Signature for security             │
   └──────┬───────────────────────────────┘
          │
          ▼
   ┌──────────────────────────────────────┐
   │  Create VerifyEmail Mailable          │
   │  - Beautiful HTML template            │
   │  - Verification button                │
   │  - Security tips                      │
   └──────┬───────────────────────────────┘
          │
          ▼

4. QUEUE PROCESSING
   ┌──────────────────────────────────────┐
   │  Email job added to queue             │
   │  (database queue)                     │
   └──────┬───────────────────────────────┘
          │
          ▼
   ┌──────────────────────────────────────┐
   │  Queue worker processes job           │
   │  php artisan queue:work               │
   └──────┬───────────────────────────────┘
          │
          ▼

5. EMAIL SENDING
   ┌──────────────────────────────────────┐
   │  Email sent via Mailtrap SMTP        │
   │  - Host: live.smtp.mailtrap.io       │
   │  - Port: 587                          │
   │  - TLS encryption                     │
   └──────┬───────────────────────────────┘
          │
          ▼
   ┌──────────────────────────────────────┐
   │  Email delivered to inbox             │
   │  kydnagpala.dev@gmail.com            │
   └──────┬───────────────────────────────┘
          │
          ▼

6. USER RECEIVES EMAIL
   ┌──────────────────────────────────────┐
   │  User opens email                     │
   │  Beautiful HTML template displayed    │
   └──────┬───────────────────────────────┘
          │
          ▼
   ┌──────────────────────────────────────┐
   │  User clicks "Verify Email" button    │
   └──────┬───────────────────────────────┘
          │
          ▼

7. VERIFICATION PROCESS
   ┌──────────────────────────────────────┐
   │  GET /email/verify/{id}/{hash}        │
   │  - Signed URL middleware validates    │
   │  - Checks signature                   │
   │  - Checks expiration                  │
   │  - Throttling: 6 attempts/min         │
   └──────┬───────────────────────────────┘
          │
          ▼
   ┌──────────────────────────────────────┐
   │  EmailVerificationController::verify  │
   │  - Check if already verified          │
   │  - Mark email as verified             │
   │  - Fire Verified event                │
   └──────┬───────────────────────────────┘
          │
          ▼

8. SUCCESS
   ┌──────────────────────────────────────┐
   │  User redirected to dashboard         │
   │  email_verified_at = now()            │
   │  Access to protected routes granted   │
   └───────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════

ALTERNATIVE FLOW: RESEND VERIFICATION EMAIL

1. USER ON VERIFICATION NOTICE PAGE
   ┌──────────────────────────────────────┐
   │  GET /email/verify                    │
   │  Verification notice page displayed   │
   └──────┬───────────────────────────────┘
          │
          ▼

2. USER CLICKS "RESEND"
   ┌──────────────────────────────────────┐
   │  POST /email/verification-notification│
   │  Rate limiting check                  │
   │  - Max 2 attempts per 5 minutes       │
   └──────┬───────────────────────────────┘
          │
          ▼
   ┌──────────────────────────────────────┐
   │  EmailVerificationController::resend  │
   │  - Check rate limit                   │
   │  - Generate new signed URL            │
   │  - Send new email                     │
   └──────┬───────────────────────────────┘
          │
          ▼

3. PROCESS REPEATS FROM STEP 4 ABOVE
   (Queue → Send → Receive → Verify)


═══════════════════════════════════════════════════════════════════════

MIDDLEWARE PROTECTION

┌──────────────────────────────────────┐
│  User tries to access protected      │
│  route (e.g., /dashboard)            │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│  'auth' middleware                    │
│  - Check if authenticated             │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│  'verified' middleware                │
│  - Check email_verified_at            │
└──────┬───────────────────────────────┘
       │
       ├─── NULL ────────┐
       │                  ▼
       │         ┌────────────────────┐
       │         │  Redirect to       │
       │         │  /email/verify     │
       │         └────────────────────┘
       │
       ├─── NOT NULL ───┐
       │                 ▼
       │         ┌────────────────────┐
       │         │  Allow access      │
       │         │  to route          │
       │         └────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│  'permission' middleware (if used)    │
│  - Check user permissions             │
└───────────────────────────────────────┘
```

## 🔧 Technical Components

### Controllers
```php
EmailVerificationController
├── notice()    → Show verification notice page
├── verify()    → Handle email verification
└── resend()    → Resend verification email
```

### Models
```php
User Model
├── implements MustVerifyEmail
├── hasVerifiedEmail()          → Check if verified
├── markEmailAsVerified()       → Mark as verified
└── email_verified_at           → Timestamp column
```

### Mailable
```php
VerifyEmail
├── $verificationUrl  → Signed URL
├── $userName        → User's name
└── view: emails.verify-email
```

### Listeners
```php
SendEmailVerificationNotification
├── implements ShouldQueue
├── handle(Registered $event)
└── Sends VerifyEmail mailable
```

### Middleware
```php
verified
├── Alias: EnsureEmailIsVerified
├── Checks: email_verified_at column
└── Redirects unverified users
```

## 📧 Email Template Structure

```html
┌─────────────────────────────────────┐
│       Purple Gradient Header        │
│    "Welcome to Laravel Starter"     │
└─────────────────────────────────────┘
│                                     │
│   Hello {User Name}!                │
│                                     │
│   Thank you for creating...         │
│   Please verify your email...       │
│                                     │
│   ┌───────────────────────────┐    │
│   │  [Verify Email Address]   │    │
│   │   (Purple Gradient Btn)   │    │
│   └───────────────────────────┘    │
│                                     │
│   ⏰ Expires in 60 minutes          │
│                                     │
│   Button not working?               │
│   Copy link: https://...            │
│                                     │
│   🔒 Security Tips                  │
│   • Don't share this link           │
│   • Link is unique to your account  │
│   • Can safely ignore if not you    │
│                                     │
│   Best regards,                     │
│   Laravel Starter Team              │
│                                     │
└─────────────────────────────────────┘
│         Gray Footer                 │
│   © 2025 Laravel Starter            │
│   Automated email - do not reply    │
└─────────────────────────────────────┘
```

## 🔐 Security Layers

```
┌────────────────────────────────────────┐
│  Layer 1: SIGNED URLs                  │
│  ✓ Signature prevents tampering        │
│  ✓ Generated with app key              │
└────────────────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────┐
│  Layer 2: EXPIRATION                   │
│  ✓ Links expire in 60 minutes          │
│  ✓ Prevents old links from working     │
└────────────────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────┐
│  Layer 3: RATE LIMITING                │
│  ✓ Resend: 2 attempts per 5 minutes    │
│  ✓ Prevents spam/abuse                 │
└────────────────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────┐
│  Layer 4: THROTTLING                   │
│  ✓ Verify: 6 attempts per minute       │
│  ✓ Prevents brute force                │
└────────────────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────┐
│  Layer 5: QUEUE PROCESSING             │
│  ✓ Async email sending                 │
│  ✓ Prevents blocking attacks           │
└────────────────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────┐
│  Layer 6: MIDDLEWARE                   │
│  ✓ Route protection                    │
│  ✓ Automatic redirection               │
└────────────────────────────────────────┘
```

## 📊 Database Schema

```sql
users table
├── id                    INTEGER PRIMARY KEY
├── name                  VARCHAR(255)
├── email                 VARCHAR(255) UNIQUE
├── email_verified_at     TIMESTAMP NULL  ← KEY COLUMN
├── password              VARCHAR(255)
├── remember_token        VARCHAR(100)
├── created_at            TIMESTAMP
└── updated_at            TIMESTAMP

Verification Status:
• email_verified_at = NULL      → Unverified
• email_verified_at = timestamp → Verified
```

## 🔄 Complete User Journey

```
1. User visits /register
2. Fills registration form
3. Submits form
4. Account created (email_verified_at = NULL)
5. Registered event fired
6. Listener queues verification email
7. Queue worker sends email via Mailtrap
8. User receives email
9. User clicks verification link
10. Server validates signed URL
11. email_verified_at set to current timestamp
12. Verified event fired
13. User redirected to dashboard
14. User can now access all protected routes
```

## ⚡ Performance Optimizations

```
┌────────────────────────────────────────┐
│  Queue Processing                      │
│  • Emails sent asynchronously          │
│  • No blocking on registration         │
│  • Better user experience              │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│  Database Indexing                     │
│  • email column indexed (UNIQUE)       │
│  • Fast verification lookups           │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│  Caching                               │
│  • Route caching enabled               │
│  • Config caching for production       │
└────────────────────────────────────────┘
```

---

**This flow diagram shows the complete email verification process from user registration to successful verification.**
